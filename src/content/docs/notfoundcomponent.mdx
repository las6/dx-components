---
title: NotFoundComponent
---

import ThemePresets from "../../components/ThemePresets";
import { NotFoundComponent } from "../../../components/NotFoundComponent";

404 page template. Dark theme, monospace, "Go home" link. Drop-in `notFoundComponent` for TanStack Router and TanStack Start.

<ThemePresets client:load>
  <NotFoundComponent />
</ThemePresets>

### Source

Source is available in the [`components/` directory on GitHub](https://github.com/las6/dx-components/tree/main/components).

### Usage

```tsx
import { createRootRoute } from "@tanstack/react-router";
import { NotFoundComponent } from "./components/NotFoundComponent";

export const Route = createRootRoute({
  notFoundComponent: NotFoundComponent,
});
```

For TanStack Start root route, wrap in the same HTML shell pattern as ErrorComponent.
